#include "ethernet.h"
/*
Ethernet::Ethernet()
{

}

bool Ethernet::encapsulate(std::vector<u8t> & payload)
{
    if(payload.size() < MIN_PAYLOAD_SIZE || payload.size() > MAX_PAYLOAD_SIZE){
        return false;
    }
				//Serial *serial = SerialManager::getInstance(SERIAL0);
				//frame.payload = payload;
    std::vector<u8t> a;
    a.reserve(sizeof(frame));
    memcpy(a.begin(), &frame, sizeof(frame));
				//dbg("Size : %u\r\n", a.size());
    return true;
}

void Ethernet::decapsulate(std::vector<u8t> & data)
{

}
*/
